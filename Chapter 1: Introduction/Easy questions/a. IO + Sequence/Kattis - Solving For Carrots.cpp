#include <bits/stdc++.h>

using namespace std;

#define IO                                  \
    freopen("../../input.txt", "r", stdin); \
    freopen("../../output.txt", "w", stdout);

int main()
{
    IO;
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n, p;
    string s;
    cin >> n >> p;

    while (n--)
    {
        cin >> s;
    }

    cout << p << "\n";

    return 0;
}